version: 2

models:
  - name: fct_receipts_to_appointments
    config:
      meta:
        dagster:
          group: core
  - name: fct_accounting_documents
    config:
      meta:
        dagster:
          group: core
